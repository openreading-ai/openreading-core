"""Reducto adapter (optional extra `reducto`; BYO API key; webhook + poll + sync)."""

from __future__ import annotations

from openreading.adapters.reducto.adapter import ReductoAdapter, ReductoClient

__all__ = ["ReductoAdapter", "ReductoClient"]
