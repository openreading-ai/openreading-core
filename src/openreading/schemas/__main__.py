"""`python -m openreading.schemas validate` — the schema self-check `make verify` runs (see `openreading.schemas.main`)."""

from __future__ import annotations

from openreading.schemas import main

if __name__ == "__main__":
    raise SystemExit(main())
